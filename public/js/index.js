fetch("/arduinoData").then( (json) => {
    console.log(json)
})

document.addEventListener('DOMContentLoaded', () => {

const option = {
    
}
    fetch("/arduinoData").then( (json) => {
        console.log(json)
    })



}